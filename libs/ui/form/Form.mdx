---
name: Form
route: /form
---

import { State } from "react-powerplug"
import { Playground } from "docz"

# Components

## Buttons

import { Button, FancyButton, VeryFancyButton } from "./Button"

<Playground>
  <FancyButton mr={2}>Save changes</FancyButton>
  <Button>Edit event</Button>
</Playground>

### Full width block buttons

<Playground>
  <FancyButton mv={3} pa={3} block>
    Register
  </FancyButton>
  <Button mv={3} block>
    Login
  </Button>
</Playground>

### Sizable via `withSpacing` props

<Playground>
  <FancyButton pa={4} mr={2}>
    Save changes
  </FancyButton>
  <Button pv={2} ph={5}>
    Edit event
  </Button>
</Playground>

### VeryFancyButton (use with care)

<Playground>
  <VeryFancyButton mv={3} pa={3} block>
    Create Account
  </VeryFancyButton>
</Playground>

## TextInput

import { TextInput } from "./TextInput"

<Playground>
  <State initial={{ value: "The Monsters" }}>
    {({ state, setState }) => (
      <TextInput
        id="test"
        label="Title"
        description="Band name, Festival name, etc."
        value={state.value}
        onChange={e => setState({ value: e.target.value })}
      />
    )}
  </State>
</Playground>

Invalid text

<Playground>
  <State initial={{ value: "" }}>
    {({ state, setState }) => (
      <TextInput
        id="test-error"
        label="Title"
        description="Band name, Festival name, etc."
        error="Please enter the name of the band"
        validate
        valid={false}
        value={state.value}
        onChange={e => setState({ value: e.target.value })}
      />
    )}
  </State>
</Playground>

Valid

<Playground>
  <State initial={{ value: "" }}>
    {({ state, setState }) => (
      <TextInput
        id="test-valid"
        label="Title"
        description="Band name, Festival name, etc."
        validate
        valid={true}
        value={state.value}
        onChange={e => setState({ value: e.target.value })}
      />
    )}
  </State>
</Playground>

import { ControlledFallbackInput } from "./ControlledFallbackInput"

## Fallback TextField for unsupported input types

Like `type="date"` in macOs Safari.

<Playground>
  <State initial={{ value: "" }}>
    {({ state, setState }) => (
      <ControlledFallbackInput
        id="test-valid"
        label="Date"
        value={state.value}
        onChange={e => setState({ value: e.target.value })}
      />
    )}
  </State>
</Playground>

## DateInput

import { DateInput } from "./DateInput"

<Playground>
  <State initial={{ value: new Date() }}>
    {({ state, setState }) => (
      <React.Fragment>
        <DateInput
          id="test"
          label="Date (DD MM YYYY)"
          value={state.value}
          onChange={(event, date) => setState({ value: date })}
        />
        {state.value.toString()}
      </React.Fragment>
    )}
  </State>
</Playground>

## TimeInput

import { TimeInput } from "./TimeInput"

<Playground>
  <State initial={{ value: new Date() }}>
    {({ state, setState }) => (
      <React.Fragment>
        <TimeInput
          id="test"
          label="Time"
          step={900 /* = 15 minutes */}
          value={state.value}
          onChange={(event, time) => setState({ value: time })}
        />
        {state.value.toString()}
      </React.Fragment>
    )}
  </State>
</Playground>

## DateTimeInput

import { DateTimeInput } from "./DateTimeInput"

<Playground>
  <State initial={{ value: new Date() }}>
    {({ state, setState }) => (
      <React.Fragment>
        <DateTimeInput
          id="test"
          dateLabel="Date"
          timeLabel="Door time"
          value={state.value}
          onChange={(event, date) => setState({ value: date })}
        />
        {state.value.toString()}
      </React.Fragment>
    )}
  </State>
</Playground>

## Select

import { Select } from "./Select"

<Playground>
  <State initial={{ value: { value: "one", label: "One" } }}>
    {({ state, setState }) => (
      <React.Fragment>
        <Select
          id="test"
          label="Numbers"
          value={state.value}
          onChange={date => setState({ value: date })}
          options={[
            { value: "one", label: "One" },
            { value: "two", label: "Two" },
            { value: "three", label: "Three" },
          ]}
        />
        {state.value.label.toString()}
      </React.Fragment>
    )}
  </State>
</Playground>

## SegmentedControl

import { SegmentedControl, SegmentedControlOption } from "./SegmentedControl"

<Playground>
  <State initial={{ value: "all" }}>
    {({ state, setState }) => (
      <SegmentedControl
        value={state.value}
        name="filter"
        onChange={e => setState({ value: e.target.value })}
      >
        <SegmentedControlOption value="all">All</SegmentedControlOption>
        <SegmentedControlOption value="mine">Mine</SegmentedControlOption>
      </SegmentedControl>
    )}
  </State>
</Playground>

<Playground>
  <State initial={{ value: "always" }}>
    {({ state, setState }) => (
      <SegmentedControl
        value={state.value}
        name="filter"
        label="Send notification on new events"
        onChange={e => setState({ value: e.target.value })}
      >
        <SegmentedControlOption value="always">Always</SegmentedControlOption>
        <SegmentedControlOption value="occasionally">
          Occasionally
        </SegmentedControlOption>
        <SegmentedControlOption value="never">Never</SegmentedControlOption>
      </SegmentedControl>
    )}
  </State>
</Playground>
